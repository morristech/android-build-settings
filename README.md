# Versioning system for kotlin projects with `gradle.kts` files
Place this module to your android project
<br>build.gradle.kts:
``` kotlin
implementation("androidx.appcompat:appcompat:${**Versions.appcompat**}")
```